# HISTORY

**언제 무엇을 했고, 무엇이 나중에 뒤집혔는지의 누적 기록이다.** 실행 시점마다 한 항목씩
쌓는다. 지난 항목은 **고치지 않는다** — 나중에 틀린 것으로 드러나면 지우는 것이 아니라
아래 「뒤집힌 것」에 한 줄을 더한다.

다른 문서와의 차이는 이렇다.

| | 무엇 | 고치나 |
| --- | --- | --- |
| **이 문서** | 시점의 누적. 연표 · 각 라운드에 무엇을 돌렸나 · 무엇이 뒤집혔나 | **안 고친다. 덧붙인다** |
| `스윕_결과.md` · `2차_스윕_결과.md` · `에폭_결과.md` · `베이스라인_기록.md` | 그 시점에 나온 **결과 본문** | 안 고친다 |
| `서버환경.md` | 지금 서버가 어떤 상태인가 | 계속 고친다 |
| `PLAN.md` · `TODO.md` | 앞으로 할 일 (git 추적 안 함) | 계속 고친다 |

---

## 연표

| 날짜 | 무엇 |
| --- | --- |
| **2026-08-11** | 저장소 시작. 채점기(`scoring.py`)를 `data_collect`에서 옮겨 심음 |
| | 학습 데이터 605건 → 학습 562 · 홀드아웃 37로 가름 |
| | **학습 전 KORMo 값을 잼**(제로샷 평균 7.6% · AM1 13.5% · negative 0건) → `베이스라인_기록.md` |
| | **1차 스윕 시작.** 설정 68개 설계 |
| **2026-08-12** | **1차 스윕 70줄 완료** → `스윕_결과.md` |
| | 채점기 `collapsed` 구멍을 고침. 실험 여섯이 잘못된 `됨` 판정을 받고 있었다 |
| | `rescore.py` 추가 — 저장된 기록만으로 다시 매긴다. GPU를 안 쓴다 |
| | 저장소를 `sft/` · `cli/` · `tests/`로 가름. 명령이 `python -m cli.train` 모양이 됨 |
| | **에폭 축 완료**(체크포인트 채점 + 6에폭 3건) → `에폭_결과.md`. **에폭은 3** |
| | 모니터링(W&B) 붙임. `_base.json`의 `report_to` |
| | 저녁: OOM 5개를 한 장에 하나씩 재시도 → `frod`만 살아남 |
| **2026-08-13** | 합성 데이터 본 생성(`data_collect`). **227건을 `TimeoutError`로 잃음** |
| **2026-08-14** | 시간 초과분 215건 복구. **홀드아웃 계열(mof) 씨앗에서 나온 합성 250건은 버리기로 결정** |
| | `sft/records.py` 두 곳 수정 — 합성분이 홀드아웃·`scores` 필터를 그냥 통과하던 자리 |
| | **학습 데이터 1,655건 확정**(562 + 합성 1,093). negative 45.9% → 15.95% |
| | `readout.py` 추가 — 홀드아웃을 사람이 눈으로 읽는 HTML 대조표 |
| | 시범 4개를 돌려 **대조군 `lora-full-rules-r2`가 96.8%로 되살아남** |
| | **2차 전면 스윕 66개 시작**(저녁). 긴 것부터 넣는 순서를 `r2-order.txt`에 박음 |
| **2026-08-18** | **2차 스윕 완료** → `2차_스윕_결과.md`. 118.4시간 |
| | 라벨일치를 채점기에 넣고 `rescore --write`로 **140줄 표 전체에 소급** |
| | `rescore`에 최소 건수 문턱, 표에 `에폭` 열 |
| | **다음 라운드를 정함: 채점기 → 데이터 증강**(6,000건) |
| | `docs/HISTORY.md` 신설. `docs/스윕_구성.md`의 내용을 흡수 |
| | **`run/` → `cli/`로 이름 바꿈.** `runs/`와 헷갈려서다. 명령이 `python -m cli.train` 모양이 됨 |
| | `cli/readout.py --sentences` 추가 — `direct_impact` 문장만 앞으로 꺼낸 판 |
| | 서버 `runs/` 456G → 48G. 체크포인트 393개(408G)를 지움 — 옵티마이저 상태라 학습이 끝나면 쓸 데가 없다 |
| | **`vera-lr1e-2` 채점.** 학습률만 올리자 AM1이 10.8% → 91.9%. **「아무것도 안 배운 6개」는 학습률 탓이 맞다** |
| | 3라운드 설정 6개(`*-lr1e-2-r2`) — 높은 학습률 × 2차 데이터. 그 여섯의 진짜 자리를 정한다 |
| | `cli/compare.py` — 문장 두 개를 눈가림으로 맞붙여 사람이 채점하는 판. Q1(모델 대 교사) 80건 · Q2(1차 대 2차) 89건 |
| | **홀드아웃 계열이 하나 늘었다** — `motie_industrial_tech_guideline_pair`(산업기술혁신사업 공통 운영요령, 159블록·141묶음). `sft/records.py`의 `HOLDOUT_SERIES`를 `frozenset`으로 바꿔 넣음(`cbf6297`). **해석이 안 붙어 데이터는 나중에 온다.** 처음부터 채점용이라 mof 때처럼 씨앗분을 뒤에서 걷어낼 일이 없다 |
| | 3라운드 6개(`*-lr1e-2-r2`) 시작(저녁). `nohup ... > log`로 띄웠다가 **로그가 안 쌓여 죽은 줄 알았다** — 파이썬 출력 버퍼였고 GPU는 돌고 있었다 |
| **2026-08-19** | **3라운드 완료.** 학습률만 올리자 여섯 중 **다섯이 형식을 배웠다**(c3a AM1 10.8% → 91.9%). **그러나 판정이 한쪽으로 쏠려 `됨`은 0개다** |
| | `rescore --write`로 표에 붙임. **140 → 147줄**(3라운드 6 + `vera-lr1e-2` 1) |
| | `check_sweep.sh` 추가 — 스윕이 **끝나서 내려갔는지 죽어서 내려갔는지**를 가린다. 표준출력의 마지막 줄 `저장: runs/sweep.md`가 판정의 주인이다 |
| | 4라운드 설정 4개 — 사이 학습률 1e-3(`c3a`·`pvera`) + `delora-sentence` seed 43·44 |
| | **채점 도중 관찰** — 모델 문장과 교사 문장을 나란히 놓으면 어느 쪽이 교사인지 거의 구분이 안 된다(사용자). **SFT는 교사가 천장이므로 그 천장에 닿았다는 뜻이다.** 남은 불만은 말투·표현인데, 교사를 잘 흉내 냈다면 **교사의 문체 문제도 같이 흉내 냈을 것**이라 SFT로는 구조적으로 못 고친다 |

---

## 각 라운드에 무엇을 돌렸나

**설정 파일(JSON) 하나가 실험 하나다.** `configs/_base.json`이 기준 조건이고, 나머지는
그것을 물려받아(`extends`) **바뀌는 칸만 적는다.** 파일 하나가 곧 그 실험의 차이 목록이다.

```json
{ "extends": "_base.json", "name": "lora-dora", "peft": { "use_dora": true } }
```

이 파일이 말하는 것은 **"기준 조건과 전부 같고 DoRA 하나만 켰다"**이다. 비교 실험이
전부 "다른 건 똑같이 두고 하나만 바꾼다"라 이렇게 한다 — 설정마다 학습률을 다 적어두면
한 곳을 고칠 때 나머지가 옛 값으로 남아 통제가 조용히 깨진다.

기준 조건(`_base.json`)은 이렇다.

```
모델     KORMo-Team/KORMo-10B-base
PEFT     LoRA r=32 · alpha=64 · dropout=0.05 · 7개 모듈 전체
학습     3에폭 · 학습률 2e-4 · 유효 배치 8 · 4K 문맥 · seed 42
정답     judgement -> labels -> impacts -> direct_impact (전체)
입력     역할 A 규칙서 전문 + 블록쌍
```

### 1차 (2026-08-11~12) — 70줄

| | 줄 | 무엇 |
| --- | ---: | --- |
| **다른 PEFT 방식** | **40** | 38종. DeLoRA만 3줄(seed 3개), 나머지 37종은 한 줄씩 |
| **LoRA 축** | **30** | LoRA는 붙는 것이 확인됐으니 그 위에서 흔든다 |

**다른 방식 40줄이 이 프로젝트의 1차 목표다.** KORMo는 `KORMoForCausalLM`이라는 자체
모델 클래스를 써서, 어떤 PEFT(원본 가중치를 얼려두고 작은 어댑터만 학습하는 방식)가
붙는지 **알려진 바가 없다.** `peft` 0.20.0이 지원하는 42종에서 LoRA(기준)와 인자 형태를
아예 모르는 것을 빼고 39종을 넣었고, DeLoRA가 유일하게 붕괴를 벗어나 seed 43·44를
뒤늦게 붙여 40줄이 됐다.

39종은 성격이 셋으로 갈린다. **갈라 두는 이유는 실패의 뜻이 다르기 때문이다.**

| 갈래 | 예 | 왜 갈라 두나 |
| --- | --- | --- |
| 구조를 가정하는 것 | IA3 · VeRA · LoHa · LoKr · OFT · TinyLoRA | 층의 모양을 전제하므로 **안 붙을 확률이 높다** |
| 처음 보는 것 | DeLoRA · RandLoRA · GraLoRA · UniLoRA · Peanut | **붙는지 자체가 정보다** |
| 프롬프트 계열 | Prompt Tuning · P-Tuning · Prefix Tuning · CPT | 어댑터가 아니라 **가상 토큰**을 앞에 붙인다 |

**터지는 것도 결과다.** 무엇이 KORMo에 못 붙는지를 그리는 것이 목적이다. 다만 "인자를
잘못 준 것"과 "진짜 안 붙는 것"은 다르므로 `cli.sweep --check`로 GPU를 쓰기 전에 한 번
거른다(`adamss`가 여기서 떨어져 표에 행이 없다 — 그래서 설계 68 + DeLoRA seed 2 +
기준 조건 1 − `adamss` 1 = 70줄이다).

**LoRA 축 30줄의 내역이다.**

| | 개수 | |
| --- | ---: | --- |
| 대조군 | 1 | 기준 조건 그대로 |
| 대조군 seed | 2 | seed 43 · 44 |
| rank | 4 | 8 · 16 · 64 · 128 (기준 32) |
| LoRA 변형 | 3 | DoRA · rsLoRA · PiSSA |
| 학습률 | 3 | 5e-5 · 1e-4 · 5e-4 (기준 2e-4) |
| **데이터 조건** | **15** | 아래 5조건 × seed 3개 |
| 조건 교차 | 2 | `no-impacts`×규칙서없음 · `sentence`×규칙서없음 |

데이터 조건 다섯은 기획서 3.3의 축 1(무엇을 학습시키나)·축 2(데이터 구성)다.

```
rules=false        규칙서 3,300자를 떼면?
target=no-impacts  중간 산출물 impacts 배열을 빼면?
target=sentence    최종 문장만 학습시키면?
negatives=drop     negative를 빼면 과대해석이 느나?
downsample=120     한 계열 쏠림(60%)을 줄이면?
```

**seed는 난수의 출발점일 뿐이고 나머지는 완전히 같다.** 홀드아웃이 37건이라 한 건이
2.7%p이므로, 두 조건이 10%p 차이 나도 진짜 차이인지 흔들림인지 seed 하나로는 말할 수
없다. **PEFT 방식에는 seed를 안 붙였다** — "붙나 안 붙나"가 사실상 예·아니오라서다.

### 2차 (2026-08-14~18) — 66줄

**조합을 새로 설계한 것이 아니다. 1차와 거의 같은 목록을 학습 데이터만 바꿔 다시 돌렸다.**

```
70 (1차) − 5 (진짜 안 붙는 것) + 1 (delora-sentence) = 66
```

**뺀 5개**는 로그의 실제 예외로 갈랐다 — `cartridge` · `cpt` · `poly` ·
`prefix-tuning` · `trainable-tokens`. 데이터를 바꿔도 안 붙는 것들이다(예: `poly`는
레코드마다 과제 번호를 요구하는데 우리 데이터에 그 칸이 없다).

**OOM 5개**(`boft`·`frod`·`ln-tuning`·`osf`·`randlora`)는 **도로 넣었다.** 방법 탓이
아니라 한 장에 둘씩 물린 탓으로 봤다. 결과적으로 `frod`만 맞았다.

**더한 1개** `delora-sentence`는 1차 표에 행이 없던 조합이다.

| | 1차 | 2차 |
| --- | ---: | ---: |
| 다른 PEFT 방식 | 40줄 (38종) | **36줄 (33종)** |
| LoRA 축 | 30줄 | **30줄** |

**LoRA 축 30줄은 1차와 완전히 같다.** 바뀐 것은 방식 쪽 개수뿐이다.

**"방법론 66가지"가 아니다.** 66줄 중 **30줄이 LoRA 하나**의 변주이고, 서로 다른 방식은
33종이며 그중 DeLoRA만 4줄(기본 · seed 43 · seed 44 · sentence)을 쓴다.

### 2차를 인용할 때 반드시 붙는 각주 셋

**`PLAN.md`에 있던 것을 2026-08-19에 옮겼다.** 그 문서는 앞으로 할 일만 두기로 했다.

- **negative가 15.95%로 기획서 상한 15%를 0.95%p 넘는다.** 알고 넘겼다 — 줄이려면 기존
  562건에서 덜어내야 하는데 그러면 1차 74건과 같은 표에 못 올린다. **비교 가능성이
  0.95%p보다 비싸다.** "상한 안이라 사고가 아니다"라는 옛 근거는 안 쓴다.
- **복합(라벨 2개 이상)이 합성분에서 4.0%인데 채점하는 홀드아웃은 34.6%다.**

  **분모를 반드시 밝힌다.** negative 건은 라벨이 0개라, 전체로 세느냐 positive로만
  세느냐에 따라 값이 두 배 갈린다.

  | | 전체 기준 | positive 기준 |
  | --- | ---: | ---: |
  | 실제 605건 | 10.1% (61/605) | 18.2% (61/335) |
  | 학습셋 1,655건 | 5.6% (93/1,655) | 6.7% (93/1,391) |
  | 그중 기존 실제분 562건 | 8.7% | 16.1% |
  | 그중 **합성분** 1,093건 | **4.0%** | **4.0%** |
  | **홀드아웃 37건** | **24.3%** (9/37) | **34.6%** (9/26) |

  **같은 기준으로 놓으면 격차가 8.6배다**(34.6% 대 4.0%). 합성분은 negative가 6건뿐이라
  두 기준이 같다.

  **격차의 원인이 둘인데 섞여 있다.** 하나는 `data_collect`의 프롬프트 규칙 6("지시한
  변경 하나만")이고 이건 일부러 그렇게 뒀다. 다른 하나는 **계열 성질**이다 — 605건을
  계열별로 세면 팁스 운영지침 25.0% · 해수부 운영규정 25.0% · 지역산업 10.1% ·
  인사혁신처 처리방침 6.6%이고, 605건의 60%가 처리방침이라 평균이 그쪽으로 끌린다.
  **홀드아웃(해수부)이 복합이 많은 것은 우연이 아니라 그 계열이 원래 그런 것이다.**

  **2차 결과를 복합 성능의 근거로 쓰지 않는다.**
- **`lora-full-down120`은 절대 건수 120을 유지한다.** 1차는 562건 중 120(21%), 2차는
  1,655건 중 120(7%)이다. 조건 이름은 같지만 뽑아 오는 못이 커졌다.

### 2차를 재현하려면 알아야 하는 것

- **`-r2` 이름의 설정 62개를 새로 만들었다.** `cli/sweep.py`가 `collect()`로
  `runs/<이름>/eval/summary.json`을 먼저 보므로, 1차와 같은 이름을 쓰면 전부 "이미 끝난
  것"으로 큐에서 빠진다. `--redo`는 1차의 어댑터와 `records.jsonl`을 덮어쓴다.
- **`extends`는 한 단계만 풀린다**(`cli/train.py`). `{"extends": "adalora.json"}`은
  `_base.json`이 안 따라온다. 그래서 원본이 덮어쓴 칸을 그대로 복사하고 `extends`는
  `_base.json`으로 적었다.
- **`_base.json`의 `data`는 안 바꿨다.** 바꾸면 1차 설정 59개가 새 데이터를 가리켜
  1차를 재현할 수 없게 된다.
- **`adalora-r2`의 `total_step`은 621이다**(1,655 ÷ 8 × 3). 1차는 562건 기준 211이었다.
  **데이터 크기에 매인 상수는 저장소에서 이것 하나뿐이고, 안 고치면 에러 없이 결과만
  조용히 달라진다.**
- **채점 홀드아웃 경로는 `cli/sweep.py`에 박혀 있다**
  (`data/20260811__annotate__v2.2/holdout.jsonl`, 37건). 학습 데이터를 바꿔도 채점은
  이 파일 위에서만 이뤄지므로 라운드가 나란히 놓인다. **건드리지 않는다.**
- 학습 데이터는 `data/20260814__annotate__v2.2-synth/`
  (train 1,655건 · holdout 37건 · 합성 원본 1,093건). 커밋 `93644eb`.

### 3차 (2026-08-18 저녁 ~ 08-19) — 6줄, 학습률 축

**1·2차에서 바닥에 깔려 있던 여섯이 「KORMo에 안 붙는 방법」인지 「학습률을 잘못 준
방법」인지를 가른다.** `fourierft` · `c3a` · `adalora` · `pvera` · `unilora` · `vera`다.

각 `-r2` 설정에서 **`learning_rate` 한 칸만** 2e-4 → 1e-2로 바꿨다. 근거는 08-18에 잰
`vera-lr1e-2` 한 건인데, 그것은 1차 데이터 위에서 돌아 형식을 배우자마자 전부 negative로
붕괴했다. **비어 있던 칸이 「높은 학습률 × 2차 데이터」다.**

| | lr 2e-4 (2차) | lr 1e-2 (3차) | 3차의 판정 분포 |
| --- | ---: | ---: | --- |
| c3a | AM1 10.8% | **91.9%** | `{'': 3, 'positive': 34}` |
| pvera | 8.1% | **70.3%** | `{'': 11, 'negative': 26}` |
| fourierft | 8.1% | **40.5%** | `{'': 22, 'positive': 15}` |
| adalora | 8.1% | **24.3%** | `{'': 28, 'positive': 4, 'negative': 5}` |
| vera | 2.7% | **13.5%** | `{'': 32, 'positive': 2, 'negative': 3}` |
| unilora | 10.8% | 10.8% | `{'': 33, 'positive': 4}` |

**여섯 중 다섯에서 형식을 배웠다. 그러나 아무도 `됨`에 못 갔다.** 2e-4에서는 말을 못
하고, 1e-2에서는 말은 하는데 한 마디만 반복한다. 넷이 쏠림 100%다.

**`c3a`의 교사일치 70.3%를 실력으로 읽으면 안 된다.** 홀드아웃이 positive 26 ·
negative 11이라 **무조건 positive면 정확히 70.3%**이고, 판정 분포가 `positive: 34`라
그 이론값 그대로다. `됨`을 못 받았는데 교사일치만 DeLoRA 다음으로 높아 보이는 칸이라
**인용할 때 분포를 반드시 같이 놓는다.**

**붕괴 방향이 데이터를 따라 뒤집혔다 — 같은 학습률에서다.**

```
1차 데이터(negative 45.9%)  + 1e-2  ->  {'': 3, 'negative': 34}   교사일치 27.0% (이론값 29.7%)
2차 데이터(negative 15.95%) + 1e-2  ->  {'': 3, 'positive': 34}   교사일치 70.3% (이론값 70.3%)
```

형식을 배우자마자 **그 데이터에서 제일 흔한 답**으로 직행한다. 「한 축을 밀면 반대쪽이
무너진다」의 세 번째 사례다.

**데이터가 붕괴를 강요하는 것은 아니다** — 같은 2차 데이터에서 `delora-r2`는
`{'': 1, 'positive': 27, 'negative': 9}`로 제대로 가른다.

**`vera`는 데이터를 늘리자 거꾸로 갔다.** 같은 1e-2에서 1차 데이터 AM1 91.9%,
2차 데이터 13.5%다. 다른 축은 같고 데이터만 다르다. `lr_scheduler_type`이 설정에 없어
기본값 `linear`가 걸리는데, 데이터가 3배면 스텝도 3배라 **1e-2로 3배 오래 미는 셈**이다.
확인하려면 스케줄러를 바꿔 다시 돌려야 하므로 조건이 하나 더 풀린다.

### 두 라운드 사이에 바뀐 것 — **학습 데이터 하나뿐이다**

```
1차   562건    negative 45.9%
2차   1,655건  negative 15.95%   (합성 1,093건을 더함)
```

방식도 학습률도 rank도 seed도 에폭(3)도 그대로다. **그래서 "붕괴 26개가 살아난 것은
데이터 때문"이라고 말할 수 있다** — 다른 것이 안 바뀌었으니 해석의 여지가 좁다.

**설정을 `-r2` 이름으로 새로 만든 이유**는 `cli/sweep.py`가 `runs/<이름>/eval/
summary.json`이 있으면 "이미 끝난 것"으로 큐에서 빼기 때문이다. 1차와 같은 이름을 쓰면
아무것도 안 돌아가고, `--redo`를 붙이면 1차의 어댑터와 기록을 덮어쓴다.

---

## 뒤집힌 것

**지난 결론이 나중에 틀린 것으로 드러난 자리다.** 원문은 그대로 두고 여기에 쌓는다 —
결과 문서를 고치면 그 라운드의 기록이 아니게 된다.

| 언제 | 무엇이라 했나 | 언제 | 어떻게 됐나 |
| --- | --- | --- | --- |
| 08-12 | **붕괴 43개는 방법 탓이다** (1차 표의 순위가 방법의 우열) | 08-18 | **아니었다.** 데이터가 판 함정이었고 데이터만 바꾸자 26개가 살아났다. 1차 순위는 "함정을 피하는 능력"의 순위였다 — 2차 1절 |
| 08-12 | **`sentence`가 `full`을 이겼다** — 1차 4절 | 08-18 | **뒤집혔다.** LoRA×sentence가 무너졌다(78.4·75.7·86.5% → 35.1·59.5·8.1%). 단 DeLoRA×sentence는 89.2%로 멀쩡하다 — 2차 4절 |
| 08-12 | **자리 부족 5개는 혼자 돌렸으면 됐을 수 있다** — 1차 8절 | 08-18 | **넷은 반증됐다.** 80GB 한 장을 혼자 줘도 OOM이다. `frod`만 진짜 자리 부족이었다 — 2차 5절 |
| 08-14 | **DeLoRA의 우위가 사라졌다. 같은 성능이면 싸고 표준적인 쪽이 이긴다** — 시범 4개를 보고 `PLAN.md`에 적음 | 08-18 | **뒤집혔다.** 시범 넷이 **전부 seed 42 한 알**이었다. seed 셋을 놓으면 DeLoRA 88.3% 대 LoRA 70.3%, 폭은 10.8%p 대 35.1%p다 — 2차 2절 |
| 08-12 | 채점기가 파싱 실패(`""`)를 판정 한 종류로 세어 **`{'': 1, 'negative': 36}`이 붕괴 검사를 빠져나갔다** | 08-12 | 같은 날 고침. 실험 여섯이 잘못된 `됨`을 받고 있었고 `rescore`로 소급했다 — 1차 7절 |
| 08-18 | 라벨일치를 넣으며 **모델이 라벨을 한 건도 안 내면 `해당 없음`**으로 짬 | 08-18 | 같은 날 고침. 그러면 **붕괴해서 라벨이 안 나온 것까지 `해당 없음`으로 숨는다**(44줄). 설정의 `target`으로 갈라 `sentence` 11줄만 남겼다 |
| 08-14 | **복합이 "실제 18% 대 합성 3.9%"** — `PLAN.md`에 적힘 | 08-18 | **못 견주는 두 값이었다.** 18%는 positive 기준, 3.9%는 전체 기준이다. 같은 기준으로 놓으면 홀드아웃 34.6% 대 합성 4.0%로 **격차가 8.6배**다. `data_collect`와 대조해 바로잡았다 |
| 08-18 | **출력 형식을 못 내는 10개는 데이터를 바꿔도 안 살아난다** — 2차 6절. `PLAN.md`의 "교사일치 0~5%짜리는 데이터를 바꿔도 안 살아날 쪽"이라는 예상이 맞았다고 적음 | 08-19 | **데이터가 아니라 학습률이었다.** `learning_rate` 한 칸만 올리자 여섯 중 다섯이 형식을 배웠다(c3a AM1 10.8% → 91.9%). **1·2차 표에서 그 자리를 "안 붙는다"로 읽은 것이 오독이다** — 3차 |
| 08-18 | **「아무것도 안 배운 6개」는 학습률 탓이 맞다** — `vera-lr1e-2` 한 건을 보고 연표에 적음 | 08-19 | **절반만 맞았다.** 학습률로 사는 것은 **형식까지**다. 다섯이 형식을 배우고도 전부 한쪽 판정으로 쏠려 `됨`은 0개다 — 3차 |
| — | **라벨일치가 산출물의 질이다**(암묵적으로) | 08-18 | **아니다**(사용자 지적). **이 과제의 산출물은 `direct_impact` 문장**이고, 라벨이 달라도 문장은 얼추 같은 경우가 흔하다. 라벨일치는 품질 점수가 아니라 눈금이다 — `TODO.md` 맨 위 |

### 사고는 아니지만 값이 움직인 것

| | |
| --- | --- |
| 합성 데이터가 **2,000건 → 약 1,700건** | 홀드아웃 계열(mof) 씨앗분 250건을 학습에서 빼고 버리기로 결정(08-14). 605 → 1,700 → 6,000 곡선의 가운데 점이 내려앉았다 |
| negative가 **13.5% → 15.95%** | 판이 작아지며 분모만 줄고 negative 개수는 그대로였다. **기획서 상한 15%를 0.95%p 넘는다 — 알고 넘겼다.** 줄이려면 1차와 데이터 조건이 달라져 같은 표에 못 올린다 |
| `hra` 소요가 **12.8시간 → 41.7시간** | 2차에서 데이터가 세 배가 되며 늘었다. 추정 23시간의 1.8배다 |

---

## 어느 기록이 어디에 있나

| | |
| --- | --- |
| 학습 전 KORMo 값 (제로샷) | `베이스라인_기록.md` |
| 1차 스윕 70줄 | `스윕_결과.md` |
| 에폭 축 (체크포인트 채점 + 6에폭) | `에폭_결과.md` |
| 2차 스윕 66줄 | `2차_스윕_결과.md` |
| 서버·판본·문맥 길이 | `서버환경.md` |
| 앞으로 할 일 | `PLAN.md` · `TODO.md` (git 추적 안 함) |
| 표 원본 | `runs/sweep.md` · `runs/sweep.json` (git 추적 안 함) |
